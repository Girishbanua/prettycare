Below is a **simple, end-to-end “Add to Cart → Place Order → Save to orders table” example** using
**HTML + CSS + JavaScript + PHP (MySQL)**.

This is **beginner-friendly**, matches your current ecommerce learning, and can be extended later.

---

## 1️⃣ Database Structure (MySQL)

### `products` table

```sql
CREATE TABLE products (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    price DECIMAL(10,2)
);
```

### `orders` table

```sql
CREATE TABLE orders (
    id INT AUTO_INCREMENT PRIMARY KEY,
    product_id INT,
    quantity INT,
    total_price DECIMAL(10,2),
    order_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 2️⃣ Database Connection (`db.php`)

```php
<?php
$conn = new mysqli("localhost", "root", "", "ecommerce");

if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}
?>
```

---

## 3️⃣ Add to Cart Page (`cart.php`)

### HTML + CSS + JavaScript

```html
<!DOCTYPE html>
<html>
<head>
    <title>Add to Cart</title>
    <style>
        body {
            font-family: Arial;
            background: #f4f6f8;
        }
        .product {
            width: 300px;
            margin: 50px auto;
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            text-align: center;
        }
        button {
            padding: 10px 15px;
            background: #007bff;
            color: white;
            border: none;
            cursor: pointer;
        }
        input {
            width: 50px;
            text-align: center;
        }
    </style>
</head>
<body>

<div class="product">
    <h2>Wireless Mouse</h2>
    <p>Price: ₹<span id="price">500</span></p>

    Quantity:
    <input type="number" id="qty" value="1" min="1">

    <br><br>
    <button onclick="placeOrder()">Place Order</button>
</div>

<script>
function placeOrder() {
    let qty = document.getElementById("qty").value;
    let price = document.getElementById("price").innerText;

    let xhr = new XMLHttpRequest();
    xhr.open("POST", "place_order.php", true);
    xhr.setRequestHeader("Content-type", "application/x-www-form-urlencoded");

    xhr.onload = function () {
        alert(this.responseText);
    };

    xhr.send("product_id=1&quantity=" + qty + "&price=" + price);
}
</script>

</body>
</html>
```

---

## 4️⃣ Order Processing (`place_order.php`)

This file **saves order to the `orders` table**.

```php
<?php
require_once "db.php";

$product_id = $_POST['product_id'];
$quantity   = $_POST['quantity'];
$price      = $_POST['price'];

$total = $quantity * $price;

$sql = "INSERT INTO orders (product_id, quantity, total_price)
        VALUES (?, ?, ?)";

$stmt = $conn->prepare($sql);
$stmt->bind_param("iid", $product_id, $quantity, $total);

if ($stmt->execute()) {
    echo "Order placed successfully!";
} else {
    echo "Error placing order";
}
?>
```

---

## 5️⃣ How It Works (Flow)

```
User clicks Place Order
↓
JavaScript sends data via AJAX
↓
PHP receives data
↓
Order saved in orders table
↓
Success message shown
```

---

## 6️⃣ What You Can Add Next 🚀

Since you’re building a **complete ecommerce site**, next steps should be:

* ✅ Session-based cart (multiple products)
* ✅ User login & attach `user_id` to orders
* ✅ `order_items` table (real ecommerce logic)
* ✅ Order summary page
* ✅ Fake payment success page

---

If you want, next I can:

* 🔥 Convert this into **session-based cart**
* 🔥 Add **login required to place order**
* 🔥 Build **full checkout page**
* 🔥 Connect with your **existing tables**

Just tell me 👍
